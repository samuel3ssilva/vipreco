name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: lint, test, build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"

      - name: Install dependencies (frozen lockfile)
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Test
        run: bun run test

      - name: Build
        run: bun run build

      - name: Verify no uncommitted or generated-file drift
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::Working tree changed after install/lint/test/build. Generated files (e.g. src/routeTree.gen.ts) or the lockfile are out of sync with the committed state."
            git status --porcelain
            git diff
            exit 1
          fi
